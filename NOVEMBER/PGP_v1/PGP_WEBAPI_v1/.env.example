# =============================================================================
# GCRegisterAPI-10-26 Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it contains secrets!
# =============================================================================

# =============================================================================
# TOKEN SECURITY (CRITICAL - GENERATE UNIQUE KEY FOR EACH ENVIRONMENT)
# =============================================================================
# Generate with: python3 -c 'import os; print(os.urandom(32).hex())'
# This key is used to sign email verification and password reset tokens
SIGNUP_SECRET_KEY=your_generated_secret_key_here_replace_this

# =============================================================================
# EMAIL SERVICE (SendGrid)
# =============================================================================
# Get API key from: https://app.sendgrid.com/settings/api_keys
# Requires "Mail Send" permission
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Email sender configuration
# Make sure FROM_EMAIL is verified in SendGrid: https://app.sendgrid.com/settings/sender_auth
FROM_EMAIL=noreply@telepay.com
FROM_NAME=TelePay

# =============================================================================
# APPLICATION URLs
# =============================================================================
# Base URL for email links (verification, password reset)
# Development: http://localhost:3000
# Production: https://app.telepay.com
BASE_URL=https://app.telepay.com

# =============================================================================
# RATE LIMITING (Optional - Production Only)
# =============================================================================
# For production, use Redis for distributed rate limiting
# Development: leave commented (uses in-memory storage)
# Production: uncomment and configure Redis connection
# REDIS_URL=redis://localhost:6379/0

# =============================================================================
# RECAPTCHA (Optional - Recommended for Production)
# =============================================================================
# Get keys from: https://www.google.com/recaptcha/admin
# Use reCAPTCHA v3 for invisible CAPTCHA
# RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here
# RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# =============================================================================
# DATABASE (Already Configured)
# =============================================================================
# Cloud SQL connection and credentials are managed via Google Secret Manager
# No changes needed here for email verification/password reset functionality

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# 1. Never commit .env to git - add it to .gitignore
# 2. Use different SIGNUP_SECRET_KEY for dev/staging/production
# 3. Rotate SIGNUP_SECRET_KEY periodically (invalidates existing tokens)
# 4. Verify FROM_EMAIL in SendGrid before sending production emails
# 5. Use strong, unique passwords for all services
# 6. Monitor SendGrid quota and usage
# =============================================================================
